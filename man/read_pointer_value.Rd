% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pointer.R
\name{read_pointer_value}
\alias{read_pointer_value}
\title{Read a value from a nested list using a JSON Pointer}
\usage{
read_pointer_value(data, pointer)
}
\arguments{
\item{data}{A named list to traverse.}

\item{pointer}{A JSON Pointer string with 1-based array indices
(as produced by S7schema error messages),
e.g. \code{"/columns/1/origin"}.}
}
\value{
The value at the pointer location, or \code{NULL} if the pointer
is invalid or the path does not exist in \code{data}.
}
\description{
Traverses a nested R list following a JSON Pointer string.
S7schema converts AJV 0-based array indices to 1-based via
\code{fix_path()} before embedding them in error messages, so the
pointer strings this function receives use 1-based indices and
can be used directly as R list positions. Tilde-escaping
(\code{~0} -> \code{~}, \code{~1} -> \code{/}) is handled per RFC 6901.
}
\examples{
data <- list(columns = list(list(origin = "Derived")))
read_pointer_value(data, "/columns/1/origin")

}
